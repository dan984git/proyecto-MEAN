const express = require('express');
const router = express.Router();
/*const ctrlMain = require('../controllers/main');*/

/* GET users listing. */
/*router.get('/', ctrlMain.usersController);*/

module.exports = router;
